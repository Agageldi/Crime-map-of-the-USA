1- index.html - main file to open project

2- three different map display method can be chosen (map, map+GU, map+GU+GU)

3- chechboxes - values can be checked and unchecked, depeendding on which whole map will be updated.
		crime checkboxes affects also choropleth map, while population checkboxes only changes GU

4- dynamic legend: 
	-changed by window and screen size
	-scrollable GU size displays
	-updated depending on the states of the checkboxes

5- state names can be seen by hovering on the state.

6- data can be read or modified in scripts/json.js file